{
  "name": "night-handover",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "npm --prefix server install --no-audit --no-fund && npm --prefix server run build && npm --prefix client install --no-audit --no-fund && npm --prefix client run build",
    "start": "node server/dist/index.js"
  }
}
