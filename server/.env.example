# Copy to .env for local dev. Never commit .env.
PORT=8080
# Google Gemini API key — used only by the ingestion (prose extraction) step.
GEMINI_API_KEY=
# Path to the structured events file loaded at startup.
DATA_PATH=../data/events.json
